<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:colorBackground">@color/background_material_light</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="divider">@color/divider</item>
        <item name="titleTextColor">@color/icons</item>
        <item name="android:textColor">@color/icons</item>
        <item name="android:textColorPrimary">@color/primary_text</item>
        <item name="android:textColorSecondary">@color/secondary_text</item>
        <item name="colorButtonNormal">@color/primary</item>
    </style>
    
    <style name="tv_places_list_title" parent="AppTheme">
        <item name="android:textColor">@color/primary_text</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">@dimen/font_size_large</item>
    </style>

    <style name="tv_place_name" parent="AppTheme">
        <item name="android:textColor">@color/primary_text</item>
        <item name="android:textSize">@dimen/font_size_normal</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="tv_place_address" parent="AppTheme">
        <item name="android:textColor">@color/secondary_text</item>
        <item name="android:textSize">@dimen/font_size_small</item>
        <item name="android:textStyle">bold</item>
        <item name="layout_gravity">fill_horizontal</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:width">0dp</item>
    </style>

    <style name="item_place_gl_grid" parent="AppTheme">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="horizontal_divider" parent="AppTheme">
        <item name="android:background">@color/divider</item>
        <item name="android:layout_width">match_parent</item>
    </style>

    <style name="tv_permission_explanation" parent="AppTheme">
        <item name="android:textSize">@dimen/font_size_normal</item>
        <item name="android:textColor">@color/primary_text</item>
        <item name="android:padding">15dp</item>
    </style>

</resources>
